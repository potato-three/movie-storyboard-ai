# ============================================
# 分镜提取项目依赖清单
# 安装方法：pip install -r requirements.txt
# ============================================

# 镜头切分（场景切换检测）
scenedetect>=0.6.4

# 视频读取与关键帧提取
opencv-python>=4.9.0

# 网页上传界面
gradio>=6.28.0,<7

# 表格数据处理
pandas>=2.0.0

# 数值计算（opencv 会附带，显式声明更稳妥）
numpy>=1.24.0

# 运动箭头图绘制中文文字 / 图像处理
pillow>=10.0.0
